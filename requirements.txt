# Build tools needed for scikit-surprise compilation
setuptools
wheel
Cython>=0.29.36,<3 # Specify Cython range if known issues exist, or just Cython

# Core libraries - Install NumPy FIRST and specifically < 2.0
numpy==1.26.4 # Or other specific 1.x version proven to work

# Main dependencies
streamlit==1.33.0
pandas==2.1.4
scikit-learn==1.4.2
scikit-surprise==1.1.3 # Try installing AFTER numpy and build tools

# Other dependencies
faker
matplotlib
seaborn
wordcloud
# Add any other direct imports from app.py
